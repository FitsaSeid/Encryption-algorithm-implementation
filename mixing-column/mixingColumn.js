import { matrixConstructor } from "../tools/tools.js"
import { matrixMultiplierConstant } from "../tools/sbox.js"

export const mixingColumn = (shiftedMatrix) => {
    // console.log(matrixConstructor(matrixMultiplierConstant))
}
