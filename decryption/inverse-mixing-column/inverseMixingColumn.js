import { matrixMultiplication } from "../../mixing-column/mixingColumn.js";

export const mixingColumn = (shiftedMatrix, inverseConstantMatrix) => matrixMultiplication(shiftedMatrix, inverseConstantMatrix);